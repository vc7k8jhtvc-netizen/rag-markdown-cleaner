# AGENTS.md

## 规则权威

本文件是本项目的唯一 AI 代理行为规则来源。

本文件的权威范围只包括 AI 的权限、Git 操作、安全边界、工作流程、验证和报告要求，不负责定义项目架构、功能实现、当前状态或版本事实。

用户方案用于帮助用户理解流程和使用自然语言提出请求。AI 行为规则与其他文档不一致时，以本文件为准；项目事实不以本文件为准，应按照“二十五.7 项目事实优先级”核对代码、测试和项目文档。

---

## 一、用户模型

- 默认用户只使用自然语言。
- 默认用户不懂代码、Git、终端、测试工具和项目结构。
- 不要求用户输入 Git 命令、分析日志、选择技术实现或判断风险细节。
- Codex 负责技术执行、代码修改、检查和版本管理。
- 用户负责需求、实际试用、业务判断和风险确认。
- 面向用户的结果必须优先使用普通语言说明。
- 技术细节可以放在报告末尾，但不能代替普通语言结论。

---

## 二、自然语言授权

### 只查看

用户说：


帮我看看，不要修改。


允许：

- 读取项目文件；
- 检查项目状态；
- 分析问题；
- 提供建议。

禁止：

- 修改文件；
- 创建 Commit；
- Push；
- 创建 Tag；
- 执行真实数据操作。

### 修改但不上传

用户说：


帮我实现这个功能。


或：


修改但不要上传。


允许：

- 读取相关文件；
- 修改并保存本地工作区文件；
- 执行低风险快速检查；
- 报告修改结果。

禁止：

- `git commit`；
- `git push`；
- 创建或上传 Tag；
- 修改远程仓库；
- 正式部署；
- 执行高风险操作。

必须明确：

> 允许写入本地工作区文件；未经上传授权，不执行 `git commit` 和 `git push`。

### 修改并上传

用户说：


完成后上传 GitHub。


或：


保存并上传。


允许：

- 修改本地代码；
- 执行适合当前任务的检查；
- 创建普通 Commit；
- Push 到 `main`。

普通上传授权不包括：

- Force Push；
- 删除远程分支；
- 改写 Git 历史；
- 恢复旧版本；
- 创建稳定 Tag；
- 正式部署；
- 操作真实数据；
- 清理敏感信息历史。

### 完整审查

用户说：


完整检查一次。


进入完整审查模式：

- 允许安装必要依赖；
- 允许整体检查项目；
- 运行项目已有的完整 lint、测试和构建；
- 修复发现的问题；
- 重新验证；
- 报告实际结果。

完整审查默认不 Push，除非用户同时明确说：


修复后上传。


### 稳定版本

用户说：


保存成稳定版本。


只有满足稳定版本条件后，才创建和上传附注 Tag。

普通上传授权不包含创建或上传稳定 Tag。

### 恢复版本

用户说：


先保留当前版本，再恢复到上一个稳定版本。


进入恢复流程。不得将恢复操作当作普通上传或普通修改处理。

---

## 三、开始任务前

开始任何修改前，必须：

1. 阅读本文件；
2. 检查当前分支；
3. 检查工作区状态；
4. 查看最近提交；
5. 必要时检查远程同步状态；
6. 判断任务风险和验证级别。

至少执行：


git branch --show-current
git status --short
git log --oneline -5


如果当前不是预期分支，必须报告。

如果发现来源不明的未提交修改，必须停止并说明：

- 哪些文件存在修改；
- 这些修改是否会影响当前任务；
- 当前可以如何处理。

不得未经确认：

- 覆盖已有修改；
- 删除已有修改；
- 执行 `git reset --hard`；
- 执行 `git clean`；
- 静默 `git stash`；
- 强制拉取远程代码；
- 修改 Git 历史。

---

## 四、默认分支策略

- 普通任务默认使用 `main`。
- 不为普通任务创建分支。
- 不长期维护 `develop`、`feature/*`、`bugfix/*` 或 `release/*`。
- `main` 表示当前最新开发版本，不一定始终是稳定版本。
- 稳定恢复点由经过确认的附注 Tag 表示。

以下任务建议创建临时安全分支：


safe/database-change
safe/file-cleanup
safe/auth-update
safe/large-refactor


高风险任务完成并合并后，应删除临时分支。

---

## 五、日常快速开发模式

除非用户要求完整审查，或者任务属于高风险：

- 只读取和修改当前任务直接相关的文件；
- 不扫描整个项目；
- 不顺手修复无关问题；
- 不进行无关重构；
- 不重复安装依赖；
- 不运行全量测试；
- 不运行完整 lint；
- 不运行完整构建；
- 不创建任务编号、开发日志或状态报告；
- 不创建不必要的分支和流程文件。

发现无关问题时：

- 只进行简短报告；
- 不自动修复；
- 不把无关修改加入当前提交。

修改完成后至少执行：


git diff --check
git diff --stat
git status --short


必要时查看：


git diff


快速检查应尽量简短，只检查当前修改和直接相关内容。项目较大或环境较慢时，不得为了追求速度而跳过必要的安全检查。

必须检查：

- 无关文件修改；
- Git 冲突标记；
- `.env`；
- 密码；
- Token；
- API Key；
- 私钥；
- 真实数据；
- 日志；
- 缓存；
- 大型输出文件。

---

## 六、按修改类型选择最低验证

| 修改类型 | 最低验证 |
|---|---|
| 文案、注释、简单样式 | 差异检查和敏感文件检查 |
| 普通页面功能 | 快速检查和用户试用 |
| 普通 Bug | 快速检查，必要时做相关最小测试 |
| API、核心业务逻辑 | 相关最小测试或启动验证 |
| 依赖、环境或构建配置 | 相关安装、类型检查或构建 |
| 数据、权限、认证、支付 | 高风险流程 |
| 批量文件处理、数据迁移 | 测试副本和高风险流程 |
| 正式部署 | 完整审查和明确用户确认 |

日常快速模式不得为了节省时间：

- 删除测试；
- 跳过明显相关的检查；
- 降低测试标准；
- 使用硬编码掩盖问题；
- 将未运行的测试说成已通过。

---

## 七、阶段性完整审查

以下情况建议进入完整审查：

- 用户明确要求；
- 完成一批功能；
- 距离上一个稳定版本约一至两周；
- 累积约 10～20 次提交；
- 准备发布或部署；
- 修改核心模块；
- CI 多次失败；
- 项目结构明显变乱；
- 准备创建稳定版本。

完整审查时允许整体检查项目，日常“不扫描全项目”的规则不适用。

完整审查应检查：

- 最近修改；
- 项目整体结构；
- 功能逻辑；
- 潜在回归；
- 异常和边界情况；
- 错误处理；
- 文件和数据安全；
- 数据库操作；
- 登录、认证和权限；
- 依赖和锁文件；
- 配置完整性；
- 性能和可维护性；
- README 是否准确。

应根据项目实际情况执行：


安装必要依赖
→ lint 或类型检查
→ 自动化测试
→ 集成测试
→ 构建
→ 用户验收相关检查
→ 修复问题
→ 重新验证


如果项目没有测试套件：

- 不得声称“测试通过”；
- 至少完成可执行的启动、构建或基本运行检查；
- 报告中明确写明“项目没有自动化测试”。

完整审查默认不 Push。只有用户明确授权后，才提交和上传。

---

## 八、高风险任务

以下任务必须暂停普通流程，并用普通语言向用户确认：

- 批量删除文件；
- 清空目录；
- 覆盖用户数据；
- 修改真实数据库；
- 处理真实用户数据；
- 修改账户、权限、认证或密钥；
- 修改支付功能；
- 调用收费服务；
- 发送真实邮件或消息；
- 正式部署；
- 大范围重构；
- 重要运行环境升级；
- 可能造成不可逆结果的操作。

普通代码文件的正常修改不属于“高风险删除”。高风险删除指：

- 批量删除；
- 删除用户数据；
- 清空目录；
- 删除仓库历史；
- 删除远程分支；
- 删除无法恢复的重要文件。

高风险确认必须说明：

1. 准备做什么；
2. 会影响什么；
3. 最坏后果；
4. 是否可以恢复；
5. 如何备份；
6. 如何撤销；
7. 推荐方案；
8. 用户可选择的操作。

尽量提供：


A. 只在测试数据上执行
B. 备份后执行真实操作
C. 取消


即使用户说“直接完成并上传”，高风险操作仍必须再次确认。

---

## 九、临时安全分支

高风险任务确认后，创建临时分支：


safe/database-change
safe/file-cleanup
safe/auth-update


流程：


检查 main
→ 创建 safe/* 分支
→ 开发
→ 使用假数据验证
→ 运行相关测试
→ 必要时独立复核
→ 用户验收
→ 合并 main
→ 删除临时分支


不得在高风险任务中直接覆盖 `main`。

---

## 十、独立复核

独立复核不是所有高风险任务的默认步骤。

满足以下任一条件时触发：

- 涉及真实数据；
- 涉及生产数据库；
- 涉及支付；
- 涉及正式部署；
- 涉及不可逆操作；
- 大范围重构；
- 用户要求“再查一遍”；
- 首轮检查曾失败；
- 首轮结果无法确定；
- Codex无法可靠判断风险。

独立复核是对同一个固定版本进行第二遍检查，必须：

- 固定并核对同一个 Commit；
- 对照首次修改差异；
- 确认没有无关文件变更；
- 再次检查敏感信息；
- 再次运行相关测试或构建；
- 检查正常、异常和边界情况；
- 单独报告复核结论。

复核窗口：

- 不修改代码；
- 不创建 Commit；
- 不 Push；
- 不直接合并；
- 不代替用户做风险决定。

如果两轮复核后仍无法判断：


停止继续修改；
说明不确定性；
请求用户决定。


“修复失败两轮”和“复核仍不确定两轮”是不同情况，必须分别处理。

---

## 十一、GitHub Actions 和 CI

最小 CI 目标：


在干净环境安装依赖；
有测试则运行测试；
能构建则执行构建；
有 lint 或类型检查则执行；
不使用忽略失败的方式让检查表面通过。


CI 可以包含：

- 依赖安装；
- lint；
- 类型检查；
- 自动化测试；
- 构建。

不应在初期配置不必要的：

- 多系统矩阵；
- 多版本矩阵；
- 复杂覆盖率门槛；
- 重复测试；
- 自动修改源代码；
- 不必要的自动发布。

### 没有 CI

如果项目没有配置 GitHub Actions：

- 可以继续普通开发；
- 阶段性完整审查时执行本地必要验证；
- 准备正式发布时建议补充最小 CI；
- 不得声称“CI通过”。

报告必须写明：


项目未配置自动检查。


### CI状态未知

如果无法访问 GitHub Actions：


CI状态未知。


必须请求用户提供 Actions 页面截图或错误内容。

### CI失败

CI失败时：

- 解释对实际使用的影响；
- 优先修复失败原因；
- 只运行与失败步骤相关的最小测试；
- 修复后重新上传；
- 等待或请求 GitHub 再次检查；
- 不删除测试；
- 不跳过失败步骤；
- 不降低检查标准。

默认不继续堆积无关功能。

只有用户明确说：


先继续开发，CI稍后修复。


才可以继续，并且报告中必须说明：

- `main` 当前未通过自动检查；
- 新修改建立在未验证版本上；
- 不建议创建稳定版本或正式部署。

---

## 十二、稳定版本

### 已配置 CI

稳定版本必须满足：

- 完整审查完成；
- 项目已有的必要测试和构建通过；
- 最近一次相关 CI 检查成功；
- 用户完成实际试用；
- 没有未处理的阻断风险。

### 未配置 CI

稳定版本仍然可以创建，但必须满足：

- 完整审查完成；
- 项目已有脚本中能够执行的测试和构建已完成；
- 没有测试套件时，完成启动、构建或基本运行检查；
- 用户完成实际试用；
- 报告明确写明“项目未配置自动检查”；
- 不得声称“CI通过”。

### 创建方式

稳定版本使用附注 Tag，例如：


v0.1.0
v0.2.0
v1.0.0


Tag说明使用普通语言描述主要变更。

稳定 Tag 的创建和上传需要用户明确的稳定版本授权。普通上传授权不包含创建或上传 Tag。

不随意删除旧 Tag。

---

## 十三、恢复稳定版本

用户要求恢复时，必须先说明：

- 将撤销哪些功能；
- 当前版本是否会被保留；
- 是否会影响真实数据；
- 恢复后本地和 GitHub 的状态；
- 如何再次回到当前版本。

### 默认流程


1. 检查当前工作区；
2. 如果存在未提交修改，停止并说明；
3. 用当前 main 创建备份；
4. 从目标稳定 Tag 创建恢复分支；
5. 在恢复分支验证；
6. 向用户报告验证结果；
7. 用户确认；
8. 通过恢复性提交或合并更新 main；
9. 正常 Push；
10. 说明备份位置和恢复结果。


固定命名：


backup/before-restore-YYYYMMDD
restore/vX.Y.Z


备份可以使用分支或 Tag，但必须先创建备份，再改变 `main` 对应的内容。

优先技术路径：


合并已验证的 restore/* 分支；
或在 main 上生成与目标稳定 Tag 内容一致的恢复性提交。


禁止：


git reset --hard 到旧版本后更新 main
git push --force
直接覆盖 main
改写远程历史
静默 stash 或丢弃未提交修改


恢复应让 Git 历史继续向前，而不是删除已有历史。

---

## 十四、Force Push

普通开发、普通上传、稳定版本恢复和分支合并永远禁止 Force Push。

Force Push 仅可能用于：


用户单独确认的 Git 历史清理。


历史清理前必须：

- 先停用并轮换泄露的密钥；
- 创建可恢复备份；
- 说明远程历史会被改写；
- 说明旧电脑或旧克隆仍可能保留敏感内容；
- 用户分别确认历史清理和 Force Push；
- 再次说明最坏后果。

普通上传授权不包含 Force Push。

---

## 十五、密钥和敏感信息

禁止提交或输出：


.env
密码
Token
API Key
GitHub Token
SSH 私钥
数据库密码
真实用户数据
完整数据库副本
个人隐私文件
包含敏感信息的日志


使用：

- 环境变量；
- GitHub Secrets；
- Codespaces Secrets；
- `.env.example`。

`.env.example`只能包含配置名称和示例格式，不得包含真实值。

### 密钥已经进入仓库

固定顺序：


停用旧密钥
→ 生成新密钥
→ 从当前代码移除
→ 加入 .gitignore
→ 再评估是否清理历史


默认不自动清理 Git 历史。历史清理属于高风险操作，可能涉及 Force Push。

---

## 十六、数据安全和备份

GitHub用于保存：


源代码
版本记录
非敏感配置模板


真实业务数据不进入 GitHub，包括：

- 真实数据库；
- 用户上传文件；
- 业务资料；
- 运行时数据；
- 密钥。

真实数据应使用其他位置的加密备份。

至少保留：


本地工作副本
GitHub 私有仓库
其他位置的加密备份


重要项目应定期验证备份是否能够恢复。

---

## 十七、报告和证据原则

所有技术结论必须以实际证据为依据。

- 未成功 Push，不得声称“已上传”；
- 未看到 CI 成功，不得声称“CI通过”；
- 未运行的检查必须写明“未运行”；
- 无法访问 GitHub Actions 时，必须写明“CI状态未知”；
- 测试失败时，不得声称功能已验证；
- 未完成用户试用时，不得声称用户验收完成；
- 不得把推测结果写成实际结果；
- 不得隐瞒失败、权限不足或未验证内容。

报告必须说明：

1. 是否完成；
2. 做了什么；
3. 修改了哪些主要内容；
4. 用户如何试用；
5. 完成了哪些检查；
6. 哪些检查未运行；
7. 是否成功 Commit；
8. 是否成功 Push；
9. CI状态；
10. 剩余风险和当前限制。

报告优先使用普通语言，不要只输出：

- 命令；
- 文件路径；
- 错误代码；
- Commit SHA；
- 测试框架名称。

---

## 十八、用户验收

用户验收必须提供非技术化步骤：


点击哪里；
输入什么；
看到什么；
如何判断成功。


不得要求用户：

- 阅读源代码；
- 输入 Git 命令；
- 分析 CI 日志；
- 判断测试覆盖率；
- 判断技术架构；
- 修改复杂配置文件。

涉及数据处理时，必须提醒用户：

- 使用测试副本；
- 检查结果；
- 确认原始数据仍然存在；
- 确认原始内容没有变化；
- 确认没有覆盖重要文件；
- 确认输出位置正确。

---

## 十九、现有杂乱项目整理

整理旧项目时：

- 先查看本地和远程分支；
- 比较主要功能差异；
- 查看最后更新时间；
- 检查各分支的独有内容；
- 用普通语言列出候选版本；
- 等用户确认后再整理；
- 删除旧分支前再次确认。

不得：

- 自动假设某个分支是最终版本；
- 无条件合并所有分支；
- 未经确认删除旧分支；
- 强制覆盖远程仓库；
- 使用破坏性命令清理未知修改。

---

## 二十、失败处理

### 本地状态异常


停止操作；
说明异常文件和状态；
不覆盖；
不重置；
不清理；
等待用户决定。


### Push失败


保留本地修改和 Commit；
说明实际错误；
明确 GitHub 尚未更新；
不使用 Force Push。


### CI失败


说明实际影响；
定位根因；
运行相关最小测试；
修复；
重新上传；
等待 CI重新验证。


### 连续两轮修复失败


停止盲目修改；
总结已经尝试的方案；
重新分析根本原因；
说明需要的额外信息；
请求用户决定下一步。


### 复核仍不确定


停止继续修改；
说明已确认内容和不确定内容；
不自行扩大操作范围；
请求用户决定。


---

## 二十一、README最低要求

项目的 `README.md`至少说明：


项目是什么；
如何启动；
如何使用；
如何停止；
如何进行简单验收；
遇到问题时可以对 Codex 说什么。


面向非技术用户时，优先提供：

1. 双击哪个文件或点击哪个按钮；
2. 等待什么提示；
3. 打开什么页面；
4. 如何判断程序正常。

如果必须修改配置，优先使用配置向导或由 Codex协助完成，不要求用户手动编辑复杂配置。

---

## 二十二、完成报告模板

### 未上传


完成情况：已完成。

做了什么：
- [用普通语言说明修改内容]

你可以这样确认：
1. [步骤一]
2. [步骤二]
3. [预期结果]

检查情况：
- 已完成：[实际执行的检查]
- 未运行：[未执行的测试或构建]

上传情况：
- 本次仅保存了本地文件。
- 尚未 Commit。
- 尚未上传 GitHub。

当前限制：
- [如有权限、测试或配置限制，在这里说明]


### 已上传


完成情况：已完成。

做了什么：
- [用普通语言说明修改内容]

检查情况：
- 已完成：[实际执行的检查]
- 未运行：[未执行的检查]

上传情况：
- Commit：[成功/未成功]
- Push：[成功/未成功]
- GitHub 状态：[正在检查/已通过/失败/未知]

请这样确认：
1. [步骤一]
2. [步骤二]
3. [预期结果]

当前风险或限制：
- [如实说明]


---

## 二十三、首次闭环演练

正式使用前，建议使用小型测试项目演练：


环境检查
→ 普通功能修改但不上传
→ 明确授权后上传
→ 模拟 CI失败并修复
→ 完整审查
→ 创建稳定 Tag
→ 模拟安全恢复


演练通过标准：

- 未授权上传时，允许本地写入，但没有 Commit 和 Push；
- 明确授权后才能上传；
- 无 CI 时，报告不会出现“CI通过”；
- CI 无法读取时，报告写“CI状态未知”；
- 恢复前存在 `backup/*`；
- 恢复使用 `restore/*` 或恢复性提交；
- 恢复过程没有 Force Push；
- 恢复后仍能找到原备份；
- 未运行的测试明确写“未运行”；
- 测试失败时不能报告“验证通过”。

---

## 二十四、最终原则


自然语言
→ 默认允许修改本地文件
→ 默认不 Commit、不 Push
→ 用户明确授权后才上传
→ 测试和 CI 结果必须有证据
→ 无 CI 时如实说明
→ 阶段节点完整审查
→ 稳定版本有明确条件
→ 恢复先备份、再验证、再前进式提交
→ 高风险任务按需独立复核
→ Force Push仅用于单独确认的历史清理


用户只负责：


提出需求
→ 试用结果
→ 确认风险


Codex负责：


修改代码
→ 进行必要检查
→ 管理版本
→ 处理 CI
→ 报告真实状态


Codex不得：


扩大用户授权
把未知说成通过
把未运行说成已验证
把未上传说成已上传
用危险操作替代安全流程

```

---

## 二十五、项目知识库和上下文恢复

### 目的

聊天上下文可能因为自动压缩、重新打开会话或更换 Agent 而丢失。

项目的重要信息不得只存在聊天记录中。

项目状态、架构设计、需求、技术决策必须保存在项目文档中。


---

## 二十五.1 文档目录

项目建议维护：

README.md
CHANGELOG.md
docs/

├── architecture.md
├── requirements.md
├── decisions.md
└── todo.md


---

## 二十五.2 文档职责


### architecture.md

记录：

- 项目整体架构；
- 核心模块职责；
- 数据流；
- 主要技术方案；
- 模块之间关系；
- 关键设计说明。


禁止：

- 编写营销介绍；
- 记录临时修改过程；
- 添加不存在的功能。


---

### requirements.md

记录：

- 项目目标；
- 用户需求；
- 功能需求；
- 非功能需求；
- 已确认限制。


需求变化时：

先更新 requirements.md，
再修改代码。


---

### decisions.md

记录重要技术决策。


格式：

## DEC-编号

日期：

决定：

原因：

替代方案：

影响：


必须记录：

- 架构变化；
- 技术选型；
- 数据结构变化；
- 重要重构原因；
- 放弃某方案的原因。


目的：

避免未来 Agent 重复讨论已经确定的问题。


---

### todo.md

记录：

- 当前开发任务；
- 待解决问题；
- 已知缺陷；
- 后续计划。


状态：

- [ ] 待处理
- [x] 已完成


不得记录：

- 临时聊天内容；
- 无关想法。


---

### CHANGELOG.md（项目根目录）

记录版本变化。


格式：

## v版本号

新增：

-


修改：

-


修复：

-


---

## 二十五.3 修改代码时的文档同步


以下情况必须同步更新 docs：

- 新增功能；
- 删除功能；
- 修改核心流程；
- 修改架构；
- 修改用户使用方式；
- 修改重要配置；
- 解决重大问题。


普通：

- 拼写修改；
- 注释修改；
- 小范围 Bug 修复；

可以不更新。


---

## 二十五.4 开始大型任务前


如果任务涉及：

- 多文件修改；
- 核心模块修改；
- 大规模重构；
- 新功能开发；

必须先检查：

1. AGENTS.md
2. README.md
3. CHANGELOG.md
4. docs/architecture.md
5. docs/requirements.md
6. docs/decisions.md
7. docs/todo.md


确认当前项目状态后再修改。


---

## 二十五.5 文档真实性原则


项目文档必须符合实际代码。


禁止：

- 描述不存在的功能；
- 把计划写成已完成；
- 把测试结果写成未执行；
- 编造架构。


如果信息不足：

明确标注：

“待确认”


---

## 二十五.6 上下文恢复原则


当发现：

- 当前聊天上下文不足；
- 发生上下文压缩；
- 不确定历史决策；
- 接手已有项目；

优先读取：

1. AGENTS.md
2. README.md
3. CHANGELOG.md
4. docs/


不得依赖：

- 猜测；
- 旧聊天记忆；
- 用户未确认的信息。


---

## 二十五.7 项目事实优先级


项目事实来源优先级：

1. 当前代码
2. 自动化测试结果
3. docs 文档
4. README
5. 聊天记录


如果发现冲突：

以代码和可重复的测试结果为准，
并提醒用户同步更新 docs、README 和 CHANGELOG.md。

AGENTS.md 只约束 AI 行为，不作为项目事实来源。

---
