# Todo

只记录有现有证据支持的后续工作。本文件不授权修改代码、调用收费 API、删除数据、上传或发布。

## P0：建立可验证基线

- [x] 2026-07-27 完整审查：本机 Python 3.13.14 + `.venv` 已安装声明开发依赖；`ruff check clean_auto tests` 通过；`pytest` 收集并通过 **345** 项。
- [x] 2026-07-27 完整审查：在临时目录构建包并通过 `twine check`。
- [x] 2026-07-27：`origin/main` @ `474a48f`（1.8.5）GitHub Actions 为 **success**。
- [x] 2026-07-27：1.8.6 候选 `29dfe40` 已 Push；完整 CI 矩阵 **success**；已创建并上传附注 Tag `v1.8.6` 与 GitHub Release。

## P0：调查已观察到的处理失败

- [x] 2026-07-28：已排除正常并发和停止流程提前关闭客户端；调度器会取消未开始任务并等待已运行 future，`ApiClient.close()` 现在还会等待活动网络流结束后再关闭底层 HTTP 客户端。
- [x] 2026-07-28：已补充“关闭中的 client + 未完成请求”专项回归，并保留停止处理、future 取消和 context manager 退出覆盖。
- [x] 2026-07-28：仓库中没有历史运行日志可供旧批次复核；已核对并补强诊断链：接口截断及 `length`/token 上限在进入质量检查前按不完整请求处理，只有正常结束的完整响应才会触发严重保留率拦截。失败日志和候选 metadata 现在记录失败阶段、响应完整性、输入输出字符数、保留率及质量报告，可区分请求截断与完整响应的提示词/模型行为。
- [x] 2026-07-28：使用合成教材片段和内存 MockTransport 完成回归验证，未读取真实资料、未调用真实 API，也未产生费用。

## P1：运行数据治理

- [x] 2026-07-28：采用生命周期保留且不按固定时间自动删除的运行数据策略；程序只在成功结果已替代诊断或复核副本后执行对应清理，其他数据分别保留到恢复、审计、复核或诊断目的完成，并在人工清理前验证工作目录外的独立备份。详见 `docs/decisions.md` 的 DEC-014 和 `使用说明.md`。

## P1：内容完整性保护

- [x] 2026-07-29：质量检查改为比较受保护正文中规范化后的数字、百分比、工程单位、标准编号和 URL 出现顺序，并增加稳定文本锚点检测；关键值变化和大范围正文替换会被严重错误阻断，既有广告行允许删除。
- [ ] 在真实但脱敏的教材样本上评估锚点阈值和单位覆盖范围；自动检查仍不能替代人工验收或支持无人值守入库。

## P1：同步文档与代码

- [x] 已确认版本事实：v1.8.0 的 workers 范围为 1-5。
- [x] 已确认版本事实：v1.8.1 将 workers 范围扩大为 1-10，并已同步 `CHANGELOG.md` 和 README。
- [x] 2026-07-27：准备发布 **1.8.6**；版本号同步到 `__init__.py`、`pyproject.toml`、README、CHANGELOG 与版本测试。
- [x] 2026-07-27：`requirements-dev.txt` 已与 `pyproject.toml` 的 dev 依赖对齐。
- [x] 2026-07-28：`development_log.md` 已标记为截至 2026-07-23 的历史归档快照；`CHANGELOG.md` 已明确版本条目是历史事实，当前版本、实现、文档和待办分别由对应权威来源提供。

## P2：平台验证

- [x] 2026-07-28：在 PowerShell 7.6.4 中完成 `menu.ps1`、`install_environment.ps1`、`select_input_files.ps1` 语法解析，并实际启动中文菜单后输入 `0` 安全退出；退出码为 0，未启动处理、未调用 API。
- [x] 2026-07-28：在 Windows PowerShell 5.1.26100 中复现并修复 `--help > file` 中文乱码。根因是 Python 管道编码为 GBK、宿主控制台输出代码页为 UTF-8；程序现在让非交互 stdout/stderr 使用实际控制台输出代码页。5.1 脚本语法、菜单启动退出及 UTF-16LE 重定向文件中文均已验证。
- [x] 远程 CI 矩阵已对 Windows 3.10/3.12/3.14 与 Ubuntu 3.10/3.12 在 `29dfe40`（1.8.6）跑通；本地完整审查在 Windows Python 3.13 复验。
- [x] 2026-07-27：确认 1.8.6 候选 SHA `29dfe40` 的完整 CI 矩阵通过。

## P2：可维护性

- [ ] 评估 `api_client.py`、`chunking.py`、`pipeline.py`、`config.py` 是否可在不改变行为的前提下拆分。
- [ ] 明确复制工作目录中 `build/`、`*.egg-info`、缓存和运行产物的清理策略；任何删除需单独确认。
- [ ] 确认是否需要接入精确 tokenizer；当前实现为近似估算。
- [x] 默认启用严格 Front Matter 校验，并保留显式 `--no-strict` 兼容模式。
- [x] 将 basedpyright 纳入 CI，以 491 条历史警告为基线阻止新增警告；后续继续按模块降低基线。

## 待确认信息

- 目标 API 提供方及模型兼容限制。
- 真实或 mock 集成测试允许的 API 费用。
- 正式使用的质量阈值。
